个人使用的python工具包，可能没多么好用
1. redhat_cve_check
    提供一串漏洞的cve号 比如CVE-2019-16935，可以自动获取是否对redhat 7 的yum包有更新，
    没有使用多线程，小心红帽告你

2. iscsi_tools
    批量做iscsi操作, login, logout, delete, discover, 获取已连接的iscsi 列表

3. press_test
    某个系统的并行压力测试脚本，修改一下又能节省几分钟。

4. config_parser
    用于对于conf配置文件解析，主要是支持重复名称的解析

5. libvirt_api_python
    反编译的libvirt api 接口规范，主要是我实在没找到这玩意在libvirt哪里能看到，倒是有c接口

6. ansible_scripts/auth_key.yml
   ansible 批量脚本，配置节点之间相互免密。
   usage: ansible-playbook -i multinode auth_key.yml

6. ansible_scripts/copy_hosts.yml
   ansible 批量脚本，全局复制hosts然后手动修改后回传。
   usage: ansible-playbook -i multinode copy_hosts.yml